name: feature-pipeline
on: [push] # Triggers the workflow on every push to the repository
jobs:
  # Job name
  feature-pipeline:
    #uses: daws-88s/workflows/.github/workflows/nodejs-feature-pipeline.yaml@main
    uses: subbu20n/dockerfiles/FROM/Dockerfile@main
    with:
      project: 'roboshop'
      component: 'catalogue'
      environment: 'dev' 
   